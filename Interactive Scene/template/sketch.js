// Interactive Scene
// Nathan Androsoff
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


function setup() {
  createCanvas(400 , 400);
}

function draw() {
  displayCircleGrid();
  background(220);
}

function displayCircleGrid() {
  //1
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //2 
  fill("red");
  ellipse(width / 2, height / 2 - 65, 75, 75);
  //3
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //4
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //5
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //6
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50); 
  //7
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //8
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50);
  //9 
  fill("red");
  ellipse(width / 2, height / 2 - 65, 50, 50); 
}